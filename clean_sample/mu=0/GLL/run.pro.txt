#!/bin/bash
#SBATCH -J GLL                  # 作业名
#SBATCH -o print.out              # 屏幕上的输出文件的名字print.out
#SBATCH -p normal                 # 作业提交的分区为 normal
#SBATCH -N 1                      # 作业申请 1 个节点
#SBATCH -w cu04                   # 指定作业节点


#SBATCH  --cpus-per-task=10       #指定cpu数量

# 设置运行环境
conda activate kwant
# 输入要执行的命令，例如 ./hello 或 python test.py 等
python "GLL.py"
